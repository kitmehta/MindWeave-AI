#! frontend-frontend-node-modules-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: frontend-frontend-node-modules
    namespace: plot-ark
    labels:
        com.docker.compose.service: frontend
        com.docker.compose.volume: frontend_node_modules
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
